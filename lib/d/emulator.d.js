import { Emulator } from '../utils/emulator.js';
import { Locker } from '../serial/locker.js';

const locker = new Locker();

Emulator.enable = true;

Emulator.status(locker);
Emulator.notDispensed(locker);
Emulator.gateInactive(locker);
Emulator.gateConfigured(locker);
Emulator.keyPressed(locker);
Emulator.doorOpened(locker);
Emulator.doorClosed(locker);
Emulator.channelDisconnected(locker);
Emulator.channelConnected(locker);
Emulator.channelEmpty(locker);
Emulator.workingTemperature(locker);
Emulator.currentTemperature(locker);
Emulator.ready(locker);
Emulator.busy(locker);
Emulator.invalidTray(locker);
Emulator.invalidChannel(locker);
Emulator.emptyChannel(locker);
Emulator.elevatorJam(locker);
Emulator.elevatorMalfunction(locker);
Emulator.phototransistorFailure(locker);
Emulator.allChannelsEmpty(locker);
Emulator.productDetectorFailure(locker);
Emulator.displayDisconnected(locker);
Emulator.productUnderElevator(locker);
Emulator.elevatorSettingAlarm(locker);
Emulator.buttonPanelFailure(locker);
Emulator.errorWritingEeprom(locker);
Emulator.errorControlTemperature(locker);
Emulator.thermometerDisconnected(locker);
Emulator.thermometerMisconfigured(locker);
Emulator.thermometerFailure(locker);
Emulator.errorExtractorConsumption(locker);
Emulator.channelSearchError(locker);
Emulator.productExitMouthSearchError(locker);
Emulator.elevatorInteriorLocked(locker);
Emulator.productDetectorVerifierError(locker);
Emulator.waitingForProductRecall(locker);
Emulator.productExpiredByTemperature(locker);
Emulator.faultyAutomaticDoor(locker);
Emulator.rejectLever(locker);
Emulator.resetCoinPurse(locker);
Emulator.coinInsertedBox(locker);
Emulator.coinInsertedTube(locker);
Emulator.banknoteInsertedStacker(locker);
Emulator.banknoteInsertedEscrow(locker);
Emulator.banknoteEjected(locker);
Emulator.banknoteInsertedRecycler(locker);
Emulator.banknoteTaken(locker);
Emulator.coinPurseEnabled(locker);
Emulator.coinPurseDisabled(locker);
Emulator.billPurseDisabled(locker);
Emulator.billPurseEnabled(locker);
Emulator.readTubes(locker);
Emulator.readBillPurse(locker);
Emulator.banknoteAccepted(locker);
Emulator.banknoteRejected(locker);
Emulator.banknotesDispensed(locker);
Emulator.coinsDispensed(locker);
Emulator.relayOn(locker);
Emulator.relayOff(locker);
Emulator.nayaxEnabled(locker);
Emulator.nayaxDisabled(locker);
Emulator.nayaxPreCreditAuthorized(locker);
Emulator.nayaxCancelRequest(locker);
Emulator.nayaxSellApproved(locker);
Emulator.nayaxSellDenied(locker);
Emulator.nayaxEndSession(locker);
Emulator.nayaxCancelled(locker);
Emulator.nayaxDispensed(locker);
Emulator.nayaxNotDispensed(locker);
Emulator.fullTray(locker);
Emulator.setConnection(locker);
